
#----------------------------------------------------------
# get the data 
#----------------------------------------------------------
compensationData<-read.table(file.choose(),header = TRUE,sep=";")
#growthData<-read.csv("growth.csv")

str(compensationData)
